import styled from 'styled-components/native';

export const ContainerButton = styled.View`
  top: -30px;
`;

export const BoxListContacts = styled.View`
  width: 327px;
  height: 457px;

  background: #ffffff;
  box-shadow: 0px 0px 8px rgba(0, 0, 0, 0.2);
  border-radius: 10px;

  top: -30px;
`;
